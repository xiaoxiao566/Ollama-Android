# 更新日志

## 1.3.10（2026-09-08）

- 修复 GPU 后端切换无效：选择 CPU 时此前注入的是 `OLLAMA_VULKAN=0`，但 termux 适配按「变量是否存在」判定启用 Vulkan，`=0` 仍会走 Vulkan，导致切不回 CPU；现在选 CPU 时彻底移除该变量
- GPU 后端默认恢复为 CPU（最稳定），需要加速时到设置页「GPU 后端」手动选择 Vulkan

## 1.3.9（2026-09-08）

- 设置页统一为液态玻璃风格，与主界面同一套视觉：渐变背景 + 柔光、磨砂返回药丸、玻璃卡片、分区绿色圆点标题、玻璃输入框
- 移除旧版靛蓝色实心标题栏与纯白卡片

## 1.3.8（2026-09-08）正式版

本次发布顶替 1.3.7 预发布版，作为正式版发布。

- **界面重绘**：去掉模板式堆卡片，改为更精致的液态玻璃布局——绿色渐变应用标识 + 副标题标题栏、状态胶囊条、启动/停止并排按钮、模型输入框与拉取按钮同行、右侧发送胶囊、分区绿色圆点标记
- **日志自动滚动**：服务输出新日志时自动滚动到底部，不再停留在原来的位置
- **Vulkan 加速修复与默认启用**：
  - GPU 后端默认改为 Vulkan（原默认 CPU，需手动切换才会走 GPU）
  - 新增 `libvulkan.so.1` 兼容桥，把系统 Vulkan 加载器复制到引擎目录，两种库名都能命中真实驱动
  - 运行日志新增 `[GPU]` 提示，明确标注「已启用 Vulkan 加速」或「未发现可用 Vulkan 设备，已回退 CPU」
- **文档**：新增 CHANGELOG.md，README 增加常见问题（Vulkan 生效确认、速度偏慢排查）

## 1.3.7（预发布）

- 新增模型运行选项：`--keepalive`（模型驻留时长）、`--system`（自定义系统提示词），以及开关型选项 `--verbose`（显示推理速度）、`--nowordwrap`、`--insecure`、`--think`、`--hidethinking`、`--experimental`、`--experimental-websearch`
- 对话末尾支持显示 tokens/s 推理速度统计

## 1.3.6

- 液态玻璃 UI：毛玻璃卡片、按钮左上受光内高光
- 设置页重构：参数分为「变量」与「高级」两级分区，高级分区点开前有警告提示
- 日志落盘：有存储权限时写入 `/storage/emulated/0/ollama-log/`，文件名 `0x_服务启动时间(全数字).log`

> 更早版本（1.3.6 之前）未保留构建记录，故更新日志从 1.3.6 开始。
